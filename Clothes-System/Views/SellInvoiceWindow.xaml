<Window x:Class="Clothes_System.Views.SellInvoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="فاتورة بيع"
        Width="700" Height="600"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="#F0F4F8">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ITEM ENTRY -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12" HorizontalAlignment="Center">
            <StackPanel>
                <Label Content="اختر النموذج" Foreground="Gray"/>
                <ComboBox Width="160"
                          Height="30"
                          ItemsSource="{Binding Models}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedModel}"/>
            </StackPanel>

            <StackPanel Margin="5,0,0,0">
                <Label Content="الكمية" Foreground="Gray"/>
                <TextBox Width="80" Height="30" Text="{Binding Quantity}"/>
            </StackPanel>

            <StackPanel Margin="5,0,0,0">
                <Label Content="سعر الوحدة" Foreground="Gray"/>
                <TextBox Width="100" Height="30" Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <Button Width="120" Height="30" Margin="10,20,0,0"
                    Background="#007ACC" Foreground="White" FontWeight="Bold"
                    Content="إضافة عنصر"
                    Command="{Binding AddItemCommand}"/>
        </StackPanel>

        <!-- ITEMS GRID -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding InvoiceItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,12"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  HorizontalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="النموذج" Binding="{Binding ModelName}" Width="*"/>
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="سعر الوحدة" Binding="{Binding UnitPrice}" Width="100"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- PAYMENT SECTION -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel>
                <Label Content="طريقة الدفع" Foreground="Gray"/>
                <ComboBox Width="160"
                          Height="30"
                          ItemsSource="{Binding PaymentTypes}"
                          SelectedItem="{Binding SelectedPaymentMethod, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Margin="10,0,0,0">
                <Label Content="المبلغ" Foreground="Gray"/>
                <TextBox Width="140" Height="30" Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </StackPanel>

        <!-- FOOTER BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,20,0,0">
            <Button Width="100" Height="35" Margin="5,0,0,0"
                    Content="إلغاء"
                    Background="#DC3545"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="Cancel_Click"/>

            <Button Width="100" Height="35" Margin="5,0,0,0"
                    Content="حفظ"
                    Background="#28A745"
                    Foreground="White"
                    FontWeight="Bold"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
