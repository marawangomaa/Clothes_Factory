<Page x:Class="Clothes_System.Views.ModelView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F7F9FC"
      FlowDirection="RightToLeft"
      Title="إدارة النماذج">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER: Title + Home Button -->
        <Grid Margin="0,0,0,15">
            <TextBlock Text="📐 انشاء موديل"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            <Button Content="🏠 العودة للصفحة الرئيسية"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="8,4"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- ADD MODEL SECTION -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="إنشاء نموذج جديد" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                    <StackPanel Margin="5">
                        <TextBlock Text="الاسم" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding Name}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="النوع" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding Type}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="الكود" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding Code}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="المتراج" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding Metrag}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="التكلفة" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding Cost}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="سعر البيع" FontWeight="Bold" Margin="0,0,0,2"/>
                        <TextBox Width="100" Text="{Binding SellPrice}" />
                    </StackPanel>

                    <Button Content="إضافة"
                    Command="{Binding AddModelCommand}"
                    Width="80"
                    Margin="5"
                    Background="#28A745"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="4,2"
                    FontWeight="Bold"
                    Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </StackPanel>
        </Border>


        <!-- MODELS DATA GRID -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Models}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionChanged="DataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="النوع" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="الكود" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="التكلفة" Binding="{Binding Cost}" Width="100"/>
                <DataGridTextColumn Header="المتراج" Binding="{Binding Metrag}" Width="100"/>
                <DataGridTextColumn Header="سعر البيع" Binding="{Binding SellPrice}" Width="100"/>
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}" Width="100"/>

                <DataGridTemplateColumn Header="الإجراءات" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="إضافة 10 قطع"
                                        Command="{Binding DataContext.AddPiecesCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Width="120"
                                        Margin="0,0,5,0"
                                        Background="#007ACC"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="4,2"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <Button Content="عرض التفاصيل"
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Width="100"
                                        Background="#28A745"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="4,2"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
