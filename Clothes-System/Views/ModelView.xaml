<Page x:Class="Clothes_System.Views.ModelView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F7F9FC"
      FlowDirection="RightToLeft"
      Title="إدارة النماذج">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER: Title + Home Button -->
        <Grid Margin="0,0,0,15">
            <TextBlock Text="📐 انشاء موديل"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            <Button Content="🏠 العودة للصفحة الرئيسية"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    FontSize="16"
                    Padding="10,6"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- ADD MODEL SECTION -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="20"
                BorderBrush="#DDD"
                BorderThickness="1"
                Margin="0,0,0,15">

            <StackPanel>
                <TextBlock Text="إنشاء نموذج جديد"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">

                    <StackPanel Margin="5">
                        <TextBlock Text="الاسم" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding Name}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="النوع" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding Type}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="الكود" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding Code}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="المتراج" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding Metrag}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="تكلفة العامل" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding MakingPrice}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="التكلفة" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding Cost}" />
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="سعر البيع" FontWeight="Bold" FontSize="16" Margin="0,0,0,3"/>
                        <TextBox Width="120" FontSize="16" Text="{Binding SellPrice}" />
                    </StackPanel>

                    <Button Content="إضافة"
                            Command="{Binding AddModelCommand}"
                            Width="100"
                            Margin="10,5,5,5"
                            Background="#28A745"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="8,4"
                            FontWeight="Bold"
                            FontSize="16"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </StackPanel>
        </Border>


        <!-- MODELS DATA GRID -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Models}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  FontSize="16"
                  SelectionChanged="DataGrid_SelectionChanged"
                  CellEditEnding="DataGrid_CellEditEnding">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="FontSize" Value="18"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="الرقم" Binding="{Binding ID}" Width="40"/>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="النوع" Binding="{Binding Type, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="الكود" Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="التكلفة" Binding="{Binding Cost, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="المتراج" Binding="{Binding Metrag, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="تكلفة العامل" Binding="{Binding MakingPrice, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="سعر البيع" Binding="{Binding SellPrice, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="120" IsReadOnly="True"/>

                <!-- Notes Column with Clickable TextBlock -->
                <DataGridTemplateColumn Header="ملاحظات" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Notes}" 
                                       TextWrapping="Wrap"
                                       MaxWidth="180"
                                       Cursor="Hand"
                                       ToolTip="انقر لتعديل الملاحظات"
                                       MouseDown="NotesTextBlock_MouseDown">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Notes}" Value="{x:Null}">
                                                <Setter Property="Text" Value="انقر لإضافة ملاحظات"/>
                                                <Setter Property="Foreground" Value="Gray"/>
                                                <Setter Property="FontStyle" Value="Italic"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Notes}" Value="">
                                                <Setter Property="Text" Value="انقر لإضافة ملاحظات"/>
                                                <Setter Property="Foreground" Value="Gray"/>
                                                <Setter Property="FontStyle" Value="Italic"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="الإجراءات" Width="280">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                                <Button Content="عرض التفاصيل"
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Width="240"
                                        Background="#28A745"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="6,3"
                                        FontSize="15"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>