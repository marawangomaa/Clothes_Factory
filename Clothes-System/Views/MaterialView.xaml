<Page x:Class="Clothes_System.Views.MaterialView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="المواد"
      Background="#F9FAFC"
      FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER: Title centered + Home Button top-right -->
        <Grid Margin="0,0,0,15">
            <TextBlock Text="📦 إدارة المواد"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            <Button Content="🏠 العودة للصفحة الرئيسية"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Add Material Section -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" VerticalAlignment="Top">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="اسم المادة" Foreground="Gray" FontSize="12"/>
                <TextBox Width="150"
                         Text="{Binding NewMaterial.Name, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="الكمية" Foreground="Gray" FontSize="12"/>
                <TextBox Width="80" Text="{Binding NewMaterial.Quantity, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="الإجمالي" Foreground="Gray" FontSize="12"/>
                <TextBox Width="100" Text="{Binding NewMaterial.TotalCost, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>


            <Button Content="شراء المادة"
                    Command="{Binding AddMaterialCommand}"
                    Background="#28A745"
                    Foreground="White"
                    Padding="10,5"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Materials Purchase History -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Materials}"
                  AutoGenerateColumns="False"
                  Margin="0,0,0,0"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  HeadersVisibility="Column"
                  BorderBrush="#DDD"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date}" Width="150" />
                <DataGridTextColumn Header="اسم المادة" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="100" />
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalCost}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
