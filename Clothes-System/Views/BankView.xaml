<Page x:Class="Clothes_System.Views.BankView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="البنك"
      Background="#F9FAFC"
      FlowDirection="RightToLeft"
      FontSize="20">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Margin="0,0,0,20">
            <Button Content="🏠 العودة للصفحة الرئيسية"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    DockPanel.Dock="Left"/>

            <TextBlock Text="🏦 البنك"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Bank Total -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15"
                BorderBrush="#DDD" BorderThickness="1">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="🏦" FontSize="40" VerticalAlignment="Center" Margin="0,0,15,0"/>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="إجمالي الرصيد" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Text="{Binding Amount, StringFormat='{}{0:C}'}"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="Green"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Transactions -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Trades}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="المتعلق بـ" Binding="{Binding RelatedTo}" Width="*" />
                <DataGridTextColumn Header="النوع" Binding="{Binding Type}" Width="120" />
                <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat='{}{0:C}'}" Width="120" />
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="160" />
                <DataGridTextColumn Header="الإجمالي بعد العملية" Binding="{Binding TotalAfterTransaction, StringFormat='{}{0:C}'}" Width="160" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>
