<Page x:Class="Clothes_System.Views.BankView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="البنك"
      Background="#F9FAFC"
      FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER: Home Button + Title -->
        <DockPanel Margin="0,0,0,20">
            <Button Content="🏠 العودة للصفحة الرئيسية"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    DockPanel.Dock="Left"/>

            <TextBlock Text="🏦 البنك"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </DockPanel>

        <!-- Bank Total Card -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15"
                BorderBrush="#DDD" BorderThickness="1" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <TextBlock Text="🏦" FontSize="36" VerticalAlignment="Center" Margin="0,0,15,0"/>

                <!-- Amount Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="إجمالي الرصيد" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Amount}" FontSize="26" FontWeight="Bold" Foreground="Green"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Transactions DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Trades}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Type}" Width="*" />
                <DataGridTextColumn Header="النوع" Binding="{Binding Type}" Width="*" />
                <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount}" Width="100" />
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}" Width="150" />
                <DataGridTextColumn Header="الإجمالي بعد العملية" Binding="{Binding TotalAfterTransaction}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
