<Page x:Class="Clothes_System.Views.StorageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="المخزن"
      FlowDirection="RightToLeft"
      Background="#F0F4F8">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Home Button -->
        <DockPanel Margin="0,0,0,15">
            <Button Content="🏠 العودة إلى الصفحة الرئيسية"
                    Width="180" Height="35"
                    Background="#007ACC" Foreground="White" FontWeight="Bold"
                    Click="Back_Click"
                    HorizontalAlignment="Left"/>
            <TextBlock Text="📦 المخزون" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#007ACC"
                       HorizontalAlignment="Center"
                       DockPanel.Dock="Top"/>
        </DockPanel>

        <!-- Storage Items DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding StorageItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#E6F0FA"
                  RowBackground="White"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  FontSize="14"
                  HorizontalAlignment="Stretch"
                  Margin="0,0,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الموديل" Binding="{Binding Product_Name}" Width="*" />
                <DataGridTextColumn Header="النوع" Binding="{Binding Product_Type}" Width="150" />
                <DataGridTextColumn Header="الكمية" Binding="{Binding Number_Of_Products}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
