<Page x:Class="Clothes_System.Views.BankUpdateView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="600"
      Title="تعديل الرصيد"
      Background="#F9FAFC"
      FlowDirection="RightToLeft"
      FontSize="20">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Margin="0,0,0,20">
            <Button Content="🔙 رجوع"
                    Click="Back_Click"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    DockPanel.Dock="Left"/>

            <TextBlock Text="✏️ تعديل رصيد البنك"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"/>
        </DockPanel>

        <!-- Update Form -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20"
                BorderBrush="#DDD" BorderThickness="1">

            <StackPanel>

                <TextBlock Text="الرصيد الحالي:" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding Amount, StringFormat='{}{0:C}'}"
                           FontSize="26"
                           Foreground="Green"
                           Margin="0,0,0,15"/>

                <TextBlock Text="الرصيد الجديد:" FontSize="18" FontWeight="Bold"/>
                <TextBox Text="{Binding NewAmount}" 
                         Width="200" Height="40"
                         FontSize="20"
                         Margin="0,5,0,10"/>

                <TextBlock Text="{Binding AmountDifference}" FontSize="16" Foreground="Gray"/>

                <Button Content="💾 حفظ التعديل"
                        Command="{Binding UpdateAmountCommand}"
                        Background="#28A745"
                        Foreground="White"
                        Padding="10"
                        Width="200"
                        Height="45"
                        Margin="0,20,0,0"/>
            </StackPanel>

        </Border>

    </Grid>
</Page>
